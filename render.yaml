services:
  - type: web
    name: chatbot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: NIXPACKS_PKGS
        value: "tesseract tesseract-lang-kaz tesseract-lang-rus"
